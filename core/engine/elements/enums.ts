export enum ElementType {
  TransformStick,
  StrokeElement,
  Image,
  Video,
  PDF,
  Text,
  Button,
  Slider,
  Rectangle,
  None,
}

export enum ElementShape {
  rectangle,
  roundedRectangle,
  ellipse,
}

export enum StrokeStyle {
  dotted,
  smooth,
}